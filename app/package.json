{
  "name": "sup-zen",
  "version": "0.0.1",
  "description": "The Zen app prototype built for the SUP program",
  "engines": {
    "node": ">=0.10.0"
  },
  "//": "node-sass is a devDependency; ideally use ruby, but this is here to remind to install it",
  "devDependencies": {
    "http-server": "^0.7.3",
    "jshint": "latest",
    "nodemon": "^1.2.1"
  },
  "scripts": {
    "build:css" : "mkdir -p dist/css && sass css/main.scss dist/css/main.css"
  },
  "dependencies": {
    "ejs": "^2.3.1",
    "express": "^4.12.3",
    "normalize.css": "^3.0.3"
  }
}
